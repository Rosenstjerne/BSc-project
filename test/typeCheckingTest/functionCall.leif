class Thing {
  var int id;
  var bool good;
}

var bool b;
var int i,j;
var Thing t;

function null f1(Thing thingy, bool compare) {
  if (thingy.good == compare) print(3);
  else print(5);
}

function Thing f2(int id, bool good) {
  var Thing thing;
  thing = new Thing;
  thing.id = id;
  thing.good = good;
  return thing;
}

function int inc(int i) {
  return i + 1;
}


i = 1;
j = 2;
b = true;

t = f2(inc(j+1),b);

f1(t,false);
